# Optional. When set, the dashboard fetches opportunity_areas.json from the
# deployed backend/ FastAPI service (see ../backend/main.py) instead of the
# local frontend/data/ snapshot. Leave unset for local dev — the local
# snapshot is used automatically.
API_URL=
